source "https://rubygems.org"

# GitHub Pages가 사용하는 gem 묶음 (로컬에서 GitHub Pages와 동일하게 빌드)
gem "github-pages", group: :jekyll_plugins

# 로컬 미리보기용 플러그인
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end

# Windows / macOS 호환용
gem "webrick"
